// Mock data layer — replace with Supabase calls when integrating the backend.
// Each export mirrors what a real data service would return.

export { mockGoals } from './goals';
export { mockLists } from './lists';
export { mockSpots } from './spots';
export { mockTags } from './tags';
export { mockChallenges } from './challenges';
export { mockUsers } from './users';
